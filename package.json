{
  "name": "rhd-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "reemdalvi.com — a static blog built from markdown with plain HTML and CSS.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node scripts/serve.mjs",
    "new:post": "node scripts/new-post.mjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "marked": "^16.0.0"
  }
}
